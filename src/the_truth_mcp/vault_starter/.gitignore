# Local environment — keep secrets out of git
.env
.envrc

# the-truth-mcp runtime files
.lock
.last_groom

# Client-specific runtime files (audit logs, caches)
.claude/audit.log
.claude/audit-*.log

# OS noise
.DS_Store
Thumbs.db

# Editor backups
*~
*.swp
*.swo
